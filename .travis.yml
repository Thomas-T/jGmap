language: java

install:
  - "mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V --settings etc/deploy-settings.xml"
after_success:
  - "mvn deploy -DskipTests --settings etc/deploy-settings.xml"
